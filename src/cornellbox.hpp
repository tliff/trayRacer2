//
// C++ Interface: cornellbox
//
// Description: 
//
//
// Author: tliff <tliff@laptop>, (C) 2008
//
// Copyright: See COPYING file that comes with this distribution
//
//
#ifndef CORNELLBOX_H
#define CORNELLBOX_H

#include <shape.hpp>

/*
	@author tliff <tliff@laptop>

class CornellBox : public Shape
{
private:
     float minX, minY, minZ;
     float maxX, maxY, maxZ;
     Color left, right, top, bottom, back;
     Color radiance;
     float lxmin, lymin, lxmax, lymax;
void updateGeometry(Vector3& const a, Vector3& const b);     
public:
  CornellBox();
  
  std::string str() const;

  virtual bool intersect(const Ray &ray,
			 Intersection* intersection);
  virtual bool doesIntersect(const Ray &ray);

  virtual void computeBoundingBox();
};
*/
#endif
